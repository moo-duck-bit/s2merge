# Core
numpy>=1.24
torch>=2.0
pyyaml>=6.0

# Router: sentence embeddings for the block and role prototypes
sentence-transformers>=2.2
transformers>=4.30

# LLM backends (the OpenAI client also talks to a local vLLM server)
openai>=1.0
tiktoken>=0.5
tenacity>=8.2
async-timeout>=4.0
python-dotenv>=1.0
nest-asyncio>=1.6

# Agents and utilities
shortuuid>=1.0
astunparse>=1.6
timeout-decorator>=0.5
requests>=2.31

# Dataset download (scripts/download_datasets.py)
datasets>=2.14

# Tests
pytest>=7.4
