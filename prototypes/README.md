# Prototype embeddings

Block and role prototypes are embedded from the descriptions in `configs/` on the
first run and cached here as `<benchmark>_block.npy` and `<benchmark>_role.npy`.
Edit a config and the stale cache is rebuilt automatically.
